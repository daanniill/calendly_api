import { DaemoClient } from "./connection/daemo-client";
import { FlexibleAgentConfig, FlexibleAgentContext, FlexibleAgentResult } from "./types/flexible";
export declare class FlexibleAgent {
    private client;
    constructor(client: DaemoClient);
    execute(prompt: string, context: FlexibleAgentContext, config?: FlexibleAgentConfig): Promise<FlexibleAgentResult & {
        virtualAction?: {
            action: string;
            payload: any;
        };
    }>;
}
