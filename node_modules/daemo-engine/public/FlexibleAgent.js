"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.FlexibleAgent = void 0;
class FlexibleAgent {
    constructor(client) {
        this.client = client;
    }
    async execute(prompt, context, config) {
        const result = await this.client.executeFlexibleAgent(prompt, context, config);
        // Check for virtual action in last tool result
        let virtualAction;
        // Virtual actions (spawn_child, etc) are plain JSON returned by tool
        if (result.lastToolResult?.virtual_action) {
            virtualAction = {
                action: result.lastToolResult.virtual_action,
                payload: result.lastToolResult.payload,
            };
        }
        return { ...result, virtualAction };
    }
}
exports.FlexibleAgent = FlexibleAgent;
