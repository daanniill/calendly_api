/**
 * DaemoBuilder - COMPLETELY FIXED VERSION
 * - Fixes parameter metadata extraction
 * - Adds explicit return type support
 * - Removes unused typeDefinitions
 * - Supports array element types
 */
import "reflect-metadata";
import { SessionData } from "../types/metadata";
export declare class DaemoBuilder {
    private serviceName;
    private systemPrompt;
    private functions;
    /**
     * Set the service name
     */
    withServiceName(serviceName: string): this;
    /**
     * Set the system prompt
     */
    withSystemPrompt(systemPrompt: string): this;
    /**
     * Register all methods from a service class
     */
    registerService<T extends object>(serviceInstance: T, namePrefix?: string): this;
    /**
     * Build and return the session data
     */
    build(): SessionData;
}
