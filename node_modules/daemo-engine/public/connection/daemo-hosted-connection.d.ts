import { DaemoHostedOptions } from "../types/config";
import { SessionData } from "../types/metadata";
export declare class DaemoHostedConnection {
    private options;
    private sessionData;
    private call;
    private isConnected;
    private shouldReconnect;
    private reconnectTimeout;
    private heartbeatInterval;
    constructor(options: DaemoHostedOptions, sessionData: SessionData);
    start(): Promise<void>;
    stop(): void;
    private connect;
    private setupEventHandlers;
    private handleDisconnection;
    private startHeartbeat;
    private authenticate;
    private registerSession;
    private handleGatewayMessage;
    private handleFunctionRequest;
    isActive(): boolean;
}
